function App() {
  return (
    <main className="page">
      <section className="card">
        <p className="eyebrow">LeGoodAnfitrion MVP</p>
        <h1>Build memorable events with better guest context.</h1>
        <p>
          This is the frontend bootstrap running in Docker. Next steps: auth, event creation, guest
          profiles and RSVP flows.
        </p>
      </section>
    </main>
  );
}

export default App;

